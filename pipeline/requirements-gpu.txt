# Qwen2.5-VL dependencies — install ON TOP of requirements.txt, on the GPU
# machine (RTX 4060 Ti):
#
#   python -m pip install -r pipeline/requirements.txt -r pipeline/requirements-gpu.txt
#
# torch + torchvision: install the CUDA builds explicitly (plain PyPI is CPU-only on Windows):
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
torch
torchvision
transformers>=4.49.0
accelerate
bitsandbytes
qwen-vl-utils
huggingface_hub
numpy
